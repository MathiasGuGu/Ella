"use client";
import Feed from "./_components/feed/dashboard-feed";
import Sidebar from "./_components/dashboard-sidebar";
import { redirect, usePathname } from "next/navigation";
import { useEffect } from "react";
const Dashboard = () => {
  // navigate to url + /
};

export default Dashboard;
